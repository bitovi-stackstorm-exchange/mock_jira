version: 1.0

input:
  - transition_data

tasks:
  create_jira_issue:
    action: jira.transition_issue
    input:
      key: <% ctx().transition_data.issue.key %>
      transition: <% ctx().transition_data.issue.transition %>