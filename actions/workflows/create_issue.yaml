version: 1.0

input:
  - alert_data

tasks:
  create_jira_issue:
    action: jira.create_issue
    input:
      summary: <% ctx().alert_data.issue.summary %>
      type: <% ctx().alert_data.issue.type %>
